rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ID validation helper
    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    // Document rule for applications
    match /applications/{applicationId} {
      allow read, write: if true;
    }

    // Document rule for support members
    match /support_members/{memberId} {
      allow read, write: if true;
    }

    // Document rule for audit logs
    match /audit_logs/{logId} {
      allow read, write: if true;
    }

    // Document rule for settings
    match /settings/{settingId} {
      allow read, write: if true;
    }

    // Document rule for test
    match /test/{testId} {
      allow read, write: if true;
    }

    // Global safety net for any other paths
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
